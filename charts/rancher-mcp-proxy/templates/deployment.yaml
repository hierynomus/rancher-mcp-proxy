apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "rancher-mcp-proxy.fullname" . }}
  labels:
    {{- include "rancher-mcp-proxy.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "rancher-mcp-proxy.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "rancher-mcp-proxy.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          env:
            - name: PORT
              value: "3000"
            - name: UPSTREAM_MCP_URL
              value: {{ .Values.upstreamMcpUrl | quote }}
            - name: RANCHER_TLS_VERIFY
              value: {{ .Values.rancherAuth.tlsVerify | quote }}
            {{- if .Values.gatewayConfig }}
            - name: ROLE_CONFIG_FILE
              value: /etc/rancher-mcp-proxy/config.yaml
            {{- else }}
            - name: REQUIRED_ROLE
              value: {{ .Values.rancherAuth.requiredRole | quote }}
            {{- end }}
            {{- with .Values.env }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- if .Values.gatewayConfig }}
          volumeMounts:
            - name: role-config
              mountPath: /etc/rancher-mcp-proxy
              readOnly: true
          {{- end }}
          livenessProbe:
            {{- toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe:
            {{- toYaml .Values.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- if .Values.gatewayConfig }}
      volumes:
        - name: role-config
          configMap:
            name: {{ include "rancher-mcp-proxy.fullname" . }}-roles
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
